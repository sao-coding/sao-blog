# docker-compose.yml (repo root)
version: "3.8"

services:
  app:
    image: sao-blog-server:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3090:3000"
    env_file: apps/server/.env
    restart: unless-stopped